FROM python:3.8-slim

WORKDIR /app

# Install required system dependencies for javalang
RUN apt-get update && apt-get install -y \
    default-jdk \
    && rm -rf /var/lib/apt/lists/*

COPY requirements.txt .
RUN pip install --no-cache-dir -r requirements.txt

COPY backend/model /app/model
COPY model/plagiarism-detector-v1.joblib /app/model/

# Add any API/service code here if needed
# COPY api.py .

EXPOSE 8000

# Update with your actual entry point
CMD ["python", "model/predict.py"]